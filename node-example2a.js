const circle = require('./node-example2.js');
console.log(`The area of a circle of radius 4 is ${circle.area(4)}`)

// before we use string + var but now use ``
// were doing 2 things a the same time 
    // creating a strign and activating the circle and excess the area from the previous file and adding 4 to calculate 
    // lof message 
    // pass the information 

    // on terminal only pass information on this file because on the other file you did not pass the information 